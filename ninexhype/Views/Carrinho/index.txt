<link rel="stylesheet" href="~/css/PagCarrinho.css">

@model ninexhype.Models.Carrinho

@{
    ViewData["Title"] = "Carrinho";
}

<div class="container">

    <div class="campoTitulo">
        <h2 class="tituloCarrinho fs-1">@ViewData["Title"]</h2>
    </div>

    @if (Model.Itens.Count == 0)
    {
        <p class="msgCarrinhoVazio fs-5">Seu carrinho está vazio 😢</p>
    }
    else
    {
        <table class="table table-striped">
            <thead>
                <tr>
                    <th>Produto</th>
                    <th>Preço</th>
                    <th>Quantidade</th>
                    <th>Total</th>
                    <th></th>
                </tr>
            </thead>
            <tbody>
                @foreach (var item in Model.Itens)
                {
                    <tr>
                        
                        <td>@item.Produto.Nome</td>
                        <td>R$ @item.Produto.ValorVenda.ToString("N2")</td>
                        <td>
                            <form asp-action="AtualizarQuantidade" method="post" class="d-flex">
                                <input type="hidden" name="itemId" value="@item.Id" />
                                <input type="number" name="novaQuantidade" value="@item.Quantidade" min="1" class="form-control w-50" />
                                <button type="submit" class="btn btn-sm btn-primary ms-2">Atualizar</button>
                            </form>
                        </td>

                        <td>R$ @(item.Produto.ValorVenda * item.Quantidade)</td>
                        <td>
                            <form asp-action="Remover" method="post">
                                <input type="hidden" name="itemId" value="@item.Id" />
                                <button type="submit" class="btn btn-sm btn-danger">Remover</button>
                            </form>
                        </td>

                    </tr>
                }
            </tbody>
        </table>

        <h4 class="totalCarrinho">Total: <strong>R$ @ViewBag.Total.ToString("N2")</strong></h4>

        <form asp-action="Limpar" method="post">
            <button type="submit" class="btn btn-warning">Limpar Carrinho</button>
        </form>
    }
</div>

